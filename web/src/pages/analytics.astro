---
import Layout from "../layouts/Layout.astro";
import AnalyticsView from "../components/AnalyticsView.tsx";
---

<Layout title="Analytics — Employee Directory">
  <h1 class="text-2xl font-semibold tracking-tight">Analytics</h1>
  <p class="mt-1 text-sm text-zinc-600">Headcount, attrition, movements — backed by <code class="rounded bg-zinc-100 px-1">/api/v1/analytics/*</code></p>

  <AnalyticsView client:load />
</Layout>
