---
import Layout from "../layouts/Layout.astro";
import Directory from "../components/Directory.tsx";
---

<Layout title="Directory — Employee Directory">
  <div class="mb-6">
    <h1 class="text-2xl font-semibold tracking-tight">Directory</h1>
    <p class="mt-1 text-sm text-zinc-600">
      Go + Fiber API at <code class="rounded bg-zinc-100 px-1">http://localhost:8080</code> — paginated, searchable, SCD2-aware.
    </p>
  </div>
  <Directory client:load />
</Layout>
